{
  "name": "beagle-azure-devops",
  "version": "1.0.0",
  "description": "To trigger Beagle penetration testing from Azure DevOps",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc --build beagle-azure-task/tsconfig.json && tfx extension create --manifest-globs vss-extension.json"
  },
  "author": "beagle",
  "license": "ISC",
  "devDependencies": {
    "tfx-cli": "^0.7.9"
  }
}
